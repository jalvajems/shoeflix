<!DOCTYPE html>
<html lang="en">
<head>
  <title>Order Confirmation | Your Store</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background-color: #f0f2f5;
      color: #333;
      line-height: 1.6;
      padding: 20px;
    }

    .container {
      max-width: 900px;
      margin: 40px auto;
      background-color: #ffffff;
      border-radius: 16px;
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
      overflow: hidden;
    }

    .order-header {
      background: linear-gradient(135deg, #34e89e 0%, #0f3443 100%);
      color: white;
      padding: 30px;
      text-align: center;
    }

    .order-header h1 {
      font-weight: 700;
      font-size: 32px;
      margin-bottom: 10px;
    }

    .order-id {
      background-color: rgba(255, 255, 255, 0.2);
      border-radius: 50px;
      padding: 8px 16px;
      font-size: 14px;
      display: inline-block;
    }

    .order-content {
      padding: 40px;
    }

    .section-title {
      color: #0f3443;
      font-weight: 600;
      margin-bottom: 20px;
      font-size: 22px;
      position: relative;
      padding-bottom: 10px;
    }

    .section-title:after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      width: 60px;
      height: 3px;
      background: linear-gradient(to right, #34e89e, #0f3443);
      border-radius: 3px;
    }

    .details-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 40px;
      flex-wrap: wrap;
    }

    .details-column {
      flex: 1;
      min-width: 250px;
      background-color: #f9f9f9;
      border-radius: 12px;
      padding: 25px;
      margin: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
    }

    .detail-label {
      font-size: 14px;
      color: #666;
      margin-bottom: 5px;
    }

    .detail-value {
      font-size: 16px;
      font-weight: 500;
      color: #333;
      margin-bottom: 15px;
    }

    .price-value {
      font-weight: 600;
      font-size: 18px;
    }

    .final-amount {
      font-weight: 700;
      font-size: 20px;
      color: #34e89e;
    }

    .discount {
      color: #e83474;
    }

    .items-container {
      margin-top: 20px;
    }

    .item-card {
      display: flex;
      align-items: center;
      margin-bottom: 20px;
      background-color: #fff;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
      transition: all 0.3s ease;
    }

    .item-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
    }

    .item-image {
      width: 100px;
      height: 100px;
      background-color: #f0f2f5;
      display: flex;
      align-items: center;
      justify-content: center;
    }

    .item-details {
      padding: 20px;
      flex: 1;
    }

    .item-name {
      font-weight: 600;
      font-size: 18px;
      margin-bottom: 5px;
    }

    .item-variant {
      font-size: 14px;
      color: #666;
      margin-bottom: 5px;
    }

    .item-price {
      font-weight: 600;
      color: #0f3443;
    }

    .action-buttons {
      display: flex;
      justify-content: center;
      margin-top: 40px;
      gap: 20px;
      flex-wrap: wrap;
    }

    .btn-download {
      background: linear-gradient(to right, #34e89e, #0f3443);
      color: white;
      border: none;
      padding: 14px 32px;
      border-radius: 50px;
      font-weight: 600;
      transition: all 0.3s ease;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 14px;
    }

    .btn-continue {
      background: #fff;
      color: #0f3443;
      border: 2px solid #0f3443;
      padding: 12px 30px;
      border-radius: 50px;
      font-weight: 600;
      transition: all 0.3s ease;
      text-transform: uppercase;
      letter-spacing: 1px;
      font-size: 14px;
    }

    .btn-download:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(15, 52, 67, 0.3);
      background: linear-gradient(to right, #30d890, #0c2c3a);
      color: white;
      text-decoration: none;
    }

    .btn-continue:hover {
      background: #0f3443;
      color: white;
      transform: translateY(-3px);
      box-shadow: 0 10px 20px rgba(15, 52, 67, 0.2);
      text-decoration: none;
    }

    .order-footer {
      margin-top: 40px;
      text-align: center;
      color: #666;
      font-size: 14px;
      padding-bottom: 20px;
    }

    @media (max-width: 768px) {
      .order-content {
        padding: 20px;
      }

      .details-column {
        min-width: 100%;
        margin: 10px 0;
      }

      .item-card {
        flex-direction: column;
      }

      .item-image {
        width: 100%;
        height: 120px;
      }

      .action-buttons {
        flex-direction: column;
        gap: 15px;
      }

      .btn-download, .btn-continue {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="order-header">
      <h1>Thank You for Your Order!</h1>
      <div class="order-id">Order ID: <%= order.orderId %></div>
    </div>
    
    <div class="order-content">
      <div class="details-row">
        <div class="details-column">
          <h3 class="section-title">Order Details</h3>
          
          <div class="detail-item">
            <div class="detail-label">Date</div>
            <div class="detail-value"><%= order.createdOn.toLocaleDateString() %></div>
          </div>
          
          <div class="detail-item">
            <div class="detail-label">Payment Method</div>
            <div class="detail-value"><%= order.paymentMethod %></div>
          </div>
          
          <div class="detail-item">
            <div class="detail-label">Status</div>
            <div class="detail-value">
              <span class="badge badge-pill badge-<%= order.status === 'Processing' ? 'warning' : 'success' %>">
                <%= order.status %>
              </span>
            </div>
          </div>
        </div>
        
        <div class="details-column">
          <h3 class="section-title">Price Summary</h3>
          
          <div class="detail-item">
            <div class="detail-label">Total Price</div>
            <div class="detail-value price-value">₹<%= order.totalPrice.toFixed(2) %></div>
          </div>
          
          <div class="detail-item">
            <div class="detail-label">Discount</div>
            <div class="detail-value price-value discount">- ₹<%= order.discount.toFixed(2) %></div>
          </div>
          
          <div class="detail-item">
            <div class="detail-label">Final Amount</div>
            <div class="detail-value final-amount">₹<%= order.finalAmount.toFixed(2) %></div>
          </div>
        </div>
      </div>
      
      <h3 class="section-title">Ordered Items</h3>
      <div class="items-container">
        <% order.orderItems.forEach(item => { %>
          <div class="item-card">
            <div class="item-details">
              <div class="item-name"><%= item.product.productName %></div>
              <div class="item-variant">Size: <%= item.variants.size %>, Quantity: <%= item.variants.quantity %></div>
              <div class="item-price">₹<%= item.price.toFixed(2) %></div>
            </div>
          </div>
        <% }); %>
      </div>
      
      <div class="action-buttons">
        <a href="/download-invoice/<%= order.orderId %>" class="btn-download">
          Download Invoice
        </a>
        <a href="/shop" class="btn-continue">
          Continue Shopping
        </a>
      </div>
      
      <div class="order-footer">
        <p>Questions about your order? Contact our support team.</p>
      </div>
    </div>
  </div>
</body>
</html>